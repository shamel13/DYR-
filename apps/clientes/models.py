from django.db import models

# El modelo Cliente se ha eliminado para consolidar los datos en el modelo Usuario.
# Las migraciones históricas conservarán la información antigua mientras se realiza la migración de datos.
